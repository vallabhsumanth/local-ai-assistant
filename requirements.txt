# Web layer (runs locally and on Railway).
fastapi
uvicorn[standard]
pydantic
python-multipart  # needed for FastAPI file uploads (the attach-file button)

# Cloud storage backend (all user data lives in Supabase, not on disk).
supabase

# Headless browser automation (also needs: playwright install chromium).
playwright

# Free web search, no API key required.
ddgs

# Spreadsheet analysis (.xlsx/.csv) — openpyxl is pandas' engine for .xlsx.
pandas
openpyxl

# Optional LLM providers — installed on demand by core/deps.py when selected:
#   anthropic   # for JARVIS_LLM_PROVIDER=anthropic
#   openai      # for JARVIS_LLM_PROVIDER=openai
#   requests    # for JARVIS_LLM_PROVIDER=ollama
requests
